# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

shared:
  API_HOST: <%= ENV["API_HOST"] || 'http://localhost' %>
  API_PORT: <%= ENV["API_PORT"] || 3001 %>

development:
  secret_key_base: 2f7de5c3963cc891bcf4b49c09880a1155b04db52e2912f7385315b164180c127020a1fd8ec963aff72c9cdb42e0b8a8acb1fc09b34fa6f0eb0e324c83d4bcdf

test:
  secret_key_base: 60a8d016016bf75a463c85b72da17ad30501be0f09c004dae1dcf38efafc219392f8a1dd6284f456578bee8d4bcc7c14e2761ffe2941f1595ceace447d7bab0a

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base : <%= ENV["SECRET_KEY_BASE"] %>
  mailgun_api_key : <%= ENV["MAILGUN_API_KEY"] %>
  mailgun_domain  : <%= ENV["MAILGUN_DOMAIN"] %>
